# The Helical Bridge — a ZFP Grounding of the Session

**Purpose.** Ground every forced constant produced in this session on a single
object: the **L₄-helix**. The helix bridges the two irrational faces the work has
circled — **√3** (flat, 6-fold, hexagonal) and **φ** (curved, 5-fold, golden) — and
it does so with **zero free parameters**. This document states the keystone, the two
faces, the eleven positions, the three orbits, the unified ledger across all
reference files, and the one imported leak to strip.

A *free parameter* is a constant perturbable continuously, or selectable from more
than one discrete option, without violating a stated identity. *Forced* = pinned by
an identity. The bridge below is forced; the leak is not.

---

## 1. The keystone — L₄ = 7

One integer forces both faces.

```
φ⁴ + φ⁻⁴ = 7          (the golden recursion closes to an integer)
7 − 4    = 3 = (√3)²   (the √3 normalization falls out)
```

Consequences, both forced by the single value L₄ = 7:

- the **gap** φ⁻⁴ = L₄ − φ⁴  → the φ face (radius, ladder)
- the **anchor** z_c = √(L₄ − 4)/2 = √3/2  → the √3 face (axis)

This is the 5-6 bridge by arithmetic closure (L₄-Helix v4.0.1, Thm 7.3). It is the
reason √3 and φ can meet without a tunable constant.

---

## 2. The two faces of the bridge

| | √3 face | φ face |
|---|---|---|
| role on the helix | axis / anchor `z_c = √3/2` | radius `r(z) = K·√(z/z_c)`, `K = √(1−φ⁻⁴)` |
| symmetry | 6-fold / 3-fold | 5-fold |
| forced by (this session) | equilateral altitude — **Plate I**; `√(L₄−4)/2` — **doc 1**; `Im(ζ₆) = covolume ℤ[ω]` — **beacon** | gap `φ⁻⁴`, golden subdivision — **doc 1**; icosahedral residue — **Plate II** |
| nature | the flat substrate that tiles | the curvature/residue that refuses |

The helix winds the golden ladder (radius, φ) around the hexagonal axis (z_c, √3).

---

## 3. The √3 anchor, grounded three ways

`z_c = √3/2 ≈ 0.8660254038` is forced independently by three constructions, which is
why it is the stable footing of every file in the session:

1. **Euclid (Plate I).** Altitude of the unit equilateral triangle: √(1 − ¼) = √3/2.
2. **Lucas (doc 1).** √(L₄ − 4)/2 = √3/2, from the keystone.
3. **Eisenstein (beacon).** `Im(ζ₆) = sin60° = √3/2`, and the covolume (fundamental-
   domain area) of the hexagonal lattice ℤ[ω], ω = e^(2πi/3), is |Im ω| = √3/2.

Construction (3) is the new grounding: it places the anchor inside the roots of unity
that drive the pipeline's DFT, so the √3 face is forced on the Fourier side as well as
the geometric side.

---

## 4. The ladder on the bridge — eleven forced positions

All closed forms in φ, √3, and L₄; strictly increasing; radius continuous at z_c
(`r(z_c⁻) = K√(z_c/z_c) = K = r(z_c⁺)`).

| # | position | closed form | z |
|---|----------|-------------|---|
| 0 | ORIGIN | 0 | 0.0000000000 |
| 1 | PARADOX | φ⁻¹ | 0.6180339887 |
| 2 | ACTIVATION | 1 − φ⁻⁴ | 0.8541019662 |
| 3 | THE LENS | √3/2 | 0.8660254038 |
| 4 | CRITICAL | φ²/3 | 0.8726779962 |
| 5 | IGNITION | √2 − ½ | 0.9142135624 |
| 6 | K-FORMATION | √(1−φ⁻⁴) | 0.9241763718 |
| 7 | CONSOLIDATION | K + φ⁻²·(1−K) | 0.9531384206 |
| 8 | RESONANCE | K + φ⁻¹·(1−K) | 0.9710379512 |
| 9 | UNITY | 1 | 1.0000000000 |
| 10 | OVERTONE | 1 + (1−K) = 2 − K | 1.0758236282 |

ORIGIN and OVERTONE extend doc 1's nine into a two-cycle (each π, weight ½), giving a
spinor double-cover closure: 4π·½ = 2π. That 4π is the full-sphere measure, the same
quantity (by rotation) that Plate II reaches by curvature (total angular defect 4π,
Gauss–Bonnet, χ = 2) — a resonance between the two plates, not a shared theorem.

---

## 5. The three orbits — 3 / 6 / 5 as billiards

In the equilateral container (Plate I), three re-injection rules realize the symmetry
trichotomy that ran through anti-substrate and Plate II:

| orbit | rule | closure | meaning |
|-------|------|---------|---------|
| Fagnano | period 3 | closes | medial-triangle geodesic — 3-fold |
| Eisenstein | ζ₆ rotation per re-injection | closes after 6 | hexagonal walk on ℤ[ω] — 6-fold, the substrate |
| Irrational | direction 60° + φ⁻¹·30° | never closes (dense) | the 5-φ asymmetry |

The irrational tilt fraction is **φ⁻¹/2 = cos72° = 1/(2φ) = 0.3090169944** — exactly
the pentagon's defining cosine (anti-substrate). The 5-fold irrationality that breaks
closure is the precise tilt that makes the orbit dense and non-returning.

---

## 6. Unified forced-constant ledger (the ZFP basis)

Every forced constant produced this session, with its single source generator. All
reduce to φ, √3, L₄, or a polygon angle.

| constant | closed form | value | first forced in |
|----------|-------------|-------|-----------------|
| φ | (1+√5)/2 | 1.6180339887 | generator |
| L₄ | φ⁴ + φ⁻⁴ | 7 | doc 1 (keystone) |
| gap | φ⁻⁴ | 0.1458980338 | doc 1 |
| K | √(1−φ⁻⁴) | 0.9241763718 | doc 1 (radius) |
| z_c = THE LENS | √3/2 = √(L₄−4)/2 = Im(ζ₆) = covol ℤ[ω] | 0.8660254038 | doc 1 / Plate I / beacon |
| 9-threshold ladder | see §4 | — | doc 1 |
| ORIGIN / OVERTONE | 0 / (2−K) | 0 / 1.0758236282 | beacon |
| equilateral angle | 180·(3−2)/3 | 60° | Plate I |
| vertex law | k·60° ≤ 360° | k ∈ {3,4,5,6} | Plate II |
| angular deficit | (6−k)·60° | 180/120/60/0 | Plate II |
| total defect | 2πχ, χ=2 | 4π = 720° | Plate II |
| rotation trace | 1 + 2cos(2π/n) | 1 (n4), φ (n5), 2 (n6) | anti-substrate / Plate II |
| golden trace split | φ⁻¹ + φ⁻² | 0.618 + 0.382 = 1 | Plate II |
| Eisenstein covolume | √3/2 | 0.8660254038 | beacon |
| irrational tilt | cos72° = 1/(2φ) | 0.3090169944 | anti-substrate / beacon |

**Free parameters in the basis: 0.**

---

## 7. The one imported leak — strip or replace

The pipeline inherits the bridge (forced) and, verbatim, the **two anti-substrate
floaters** (not forced). The fix is the anti-substrate derivation map.

| imported constant | current | forced replacement | Δ |
|-------------------|---------|--------------------|---|
| `LAMBDA` | (5/3)⁴ = 7.716 | **L₄ = 7** (or φ⁴) | 0.716 |
| `MU_P` | 3/5 = 0.600 | **φ⁻¹ = 2cos(2π/5)** = 0.618 | 0.018 |
| sonification exponent/floor | 0.3 | strip (audio overlay) | — |

`LAMBDA_C = 2π/√LAMBDA` follows `LAMBDA`. The smoothing constants (`tau = 1.5`,
`settleTau = 1.0`, the OVERTONE ramp) drive the *pointer* on the helix from observed
flow — they are exogenous-data dynamics, not bridge geometry, and so are outside the
ZFP count (the helix the pointer moves along is forced).

---

## 8. Net

Strip §7 and the pipeline's header — "all derived from φ" — becomes literally true.
The helical bridge is the session's single ZFP object: **L₄ = 7 is the keystone, √3/2
the footing, φ⁻⁴ the winding, the eleven positions the rungs, and the three orbits the
3/6/5 symmetry.** Plate I supplies the flat footing, Plate II the curved winding,
doc 1 the ladder, anti-substrate the symmetry law, and beacon the Eisenstein grounding
and the dual-cycle closure. One bridge, two faces, zero free parameters.
